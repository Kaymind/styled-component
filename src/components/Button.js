import React from "react";

import ButtonComponent from "./button.style.component";

const Button = ({ children }) => {
  return <ButtonComponent>{children}</ButtonComponent>;
};

export default Button;
