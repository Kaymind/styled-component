import React from "react";

import MainCard from "./maincard.style.component";

const MainCardComponent = ({ children }) => {
  return <MainCard>{children}</MainCard>;
};

export default MainCardComponent;
